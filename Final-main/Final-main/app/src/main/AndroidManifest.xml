<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.final_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_App">
        <activity android:name=".study"></activity>
        <activity android:name=".study_flashcards" />
        <activity android:name=".EditAddGroupAct" />
        <activity android:name=".EditFlashCardsAct" />
        <activity android:name=".SelectGroupAct" />
        <activity android:name=".FlashcardActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".splashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>